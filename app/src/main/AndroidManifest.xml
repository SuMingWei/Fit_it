<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitit">
        <activity android:name=".Endurance_exercise"></activity>
        <activity android:name=".Softness_exercise" />
        <activity android:name=".Lower_exercise" />
        <activity android:name=".Upper1_exercise" />
        <activity android:name=".Mail" />
        <activity android:name=".Diary" />
        <activity android:name=".Mission" />
        <activity android:name=".Home" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>